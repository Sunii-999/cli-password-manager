[package]
name = "cli-password-manager"
version = "0.1.0"
edition = "2024"

# Cargo.toml
[dependencies]
argon2 = "0.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"

