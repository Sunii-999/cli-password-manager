[package]
name = "cli-password-manager"
version = "0.1.0"
edition = "2024"

# Cargo.toml
[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
aes-gcm = "0.10"
rand = "0.8"
base64 = "0.21"